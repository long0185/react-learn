import React, { ReactElement } from 'react'

interface Props {
    
}

export default function welcome({}: Props): ReactElement {
    return (
        <div>
            <h1>welcome</h1>
        </div>
    )
}
